[package]
name = "openbrain-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "openbrain"
path = "src/main.rs"

[dependencies]
openbrain-core = { path = "../openbrain-core" }
